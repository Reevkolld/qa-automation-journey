// =============================================================
// 03 — this в JS (4 правила)
// Запуск: node 03-this-binding.js

// Задача 1. Метод объекта vs «оторванный» метод
// что выведет user.greet()? Oleksandr
// а оторванный greet()? undefined
const user = {
  name: "Oleksandr",
  greet() {
    return `Hi, I'm ${this.name}`;
  },
};
console.log("1a:", user.greet());
const g = user.greet;
console.log("1b:", g());

// Задача 2. Стрелочная функция берёт this из окружения
const obj = {
  name: "obj",
  regular: function () {
    return this && this.name; //obj
  },
  arrow: () => {
    return this && this.name; // undefined, т.к. стрелочная функция не имеет своего this
  },
};
console.log("2a:", obj.regular());
console.log("2b:", obj.arrow());

// Задача 3. Явная привязка через bind
const user2 = { name: "Bound User" };
const boundGreet = user.greet.bind(user2);
console.log("3:", boundGreet());

// -------------------------------------------------------------
// Задача 4 Почему это важно в Playwright/колбэках:
// Стрелочные функции в колбэках сохраняют this теста — допиши вывод,
// зачем тебе это знать как автотестеру: чтобы не потерять контекст теста и иметь доступ к его методам
// -------------------------------------------------------------

// Вывод по всем 4 правилам this:
// 1) обычный вызов  - this ссылается на глобальный объект (window в браузере, global в Node.js) или undefined в строгом режиме
// 2) метод объекта - this ссылается на объект, которому принадлежит метод
// 3) явная привязка (call/apply/bind)  - this ссылается на объект, переданный в качестве первого аргумента
// Пример:
// array.forEach(function(item) { console.log(this); }, obj); // this будет ссылаться на obj
// 4) стрелочная (лексический this) - this ссылается на контекст, в котором была создана стрелочная функция, и не имеет собственного this.
// Пример: если стрелочная функция создана внутри метода объекта, то this будет ссылаться на объект, которому принадлежит метод.
