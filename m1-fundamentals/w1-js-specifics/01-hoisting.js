// =============================================================
// 01 — Hoisting, var/let/const, TDZ
// Запуск: node 01-hoisting.js
// Правило игры: СНАЧАЛА впиши предсказание в "// ПРЕДСКАЗАНИЕ:",
// потом раскомментируй код, запусти и сравни.
// =============================================================

// -------------------------------------------------------------
// Задача 1. var всплывает как undefined
// ПРЕДСКАЗАНИЕ: что выведет console.log(a)? _______
// -------------------------------------------------------------
console.log("1:", a);
var a = 10;

// -------------------------------------------------------------
// Задача 2. let в TDZ
// ПРЕДСКАЗАНИЕ: что произойдёт на console.log(b)? _______
// (undefined / ReferenceError / 20)
// -------------------------------------------------------------
let b = 20;
console.log("2:", b);


// -------------------------------------------------------------
// Задача 3. Область видимости var vs let в блоке
// ПРЕДСКАЗАНИЕ: что выведут обе строки после блока? _______
// -------------------------------------------------------------
{
  var x = "var внутри блока";
  let y = "let внутри блока";
}
console.log("3a:", x); // доступна ли x снаружи?
// console.log("3b:", y); // доступна ли y снаружи?

// -------------------------------------------------------------
// Задача 4. const и мутация
// ПРЕДСКАЗАНИЕ: какая строка упадёт, какая отработает? _______
// -------------------------------------------------------------
const arr = [1, 2];
arr.push(3);      // (a) ошибка или ок?
arr = [9, 9, 9];  // (b) ошибка или ок?
console.log("4:", arr);

// -------------------------------------------------------------
// Задача 5. Hoisting объявления функции vs выражения
// ПРЕДСКАЗАНИЕ: какой вызов сработает, какой упадёт? _______
// -------------------------------------------------------------
console.log("5a:", sum(2, 3));   // function declaration
// console.log("5b:", mul(2, 3));   // function expression в const

function sum(p, q) {
  return p + q;
}
const mul = (p, q) => p * q;

// -------------------------------------------------------------
// ВЫВОД (заполни после прогона):
// Что меня удивило? _______
// Чем JS-var отличается от C#-var? _______
// -------------------------------------------------------------
